# Architecture

This document has moved to **[`../ARCHITECTURE.md`](../ARCHITECTURE.md)** at the
repository root — the comprehensive architecture spec: crate layering, the
single-writer runtime model, the data-flow paths, and the
consensus/persistence/reorg contracts.

For per-crate detail (modules, key types, "start here"), see the
[codebase map](./codemap.md).
