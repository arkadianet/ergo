# Provisioned builds. One work directory per build (see builds.toml);
# `.work` is the unsuffixed default.
.work*
# logback file appender of the pinned ergo build: the harness JVM writes it
# into whatever directory gen.py launches from.
ergo.log
